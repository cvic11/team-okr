# 테스트 가이드 — 런칭 전 통합 검증

팀원들이 실사용에서 반복적으로 부족함을 발견하는 문제를 줄이기 위한 검증 체계입니다.
**푸시 전 항상 `node .preflight.js` 를 실행**하세요 (8단계 자동 검증).

```bash
node .preflight.js     # 문법·스코프·TDZ + 3개 테스트 스위트 일괄 실행
```

## 자동 테스트 (순수 로직 — 회귀 차단)

| 파일 | 범위 | 케이스 |
|---|---|---|
| `.test-tasks.js` | 오늘 탭 이니셔티브/할일 가시성·트리 전수 | 215 |
| `.test-integration.js` | 오늘 작성·최근 한 일 연동·**timezone 경계**·상호배제 | 25 |
| `.test-suite.js` | 진척률·날짜·분기/WBS·검색·권한/소유·집계·루틴·날짜전파·직렬화 | 82 |

실제 `app.js` 함수를 소스 그대로 추출(vm)해 검증하므로, 로직 수정 시 테스트가 함께 깨져 회귀를 잡습니다.
모든 날짜 테스트는 **KST(Asia/Seoul) 고정** 실행 — UTC/로컬 경계 버그를 재현합니다.

### 새 기능 추가 시
1. 해당 도메인 테스트 파일에 케이스 추가 (작성→가시성, 경계값, 권한 분기)
2. 순수 로직은 가능하면 **named function**으로 분리해 추출·테스트 가능하게
3. `node .preflight.js` 초록 확인 후 푸시

## 수동 QA 체크리스트 (자동화 어려운 상호작용)

자동 테스트가 못 잡는 영역 — 런칭 전/주요 변경 후 1회 점검 권장.

### 오늘·스탠드업
- [ ] **오전 9시 이전** 할일 작성 → 새로고침/다음날 아침에도 '오늘 할 일'에 유지 (timezone)
- [ ] 할일 작성 직후 제목 입력 → 항목이 사라지지 않음
- [ ] '오늘' 탭 클릭 → 날짜 박스가 당일로 복귀
- [ ] 날짜 박스 어디를 눌러도(월/일/터치 포함) 달력 열림
- [ ] 마감 지난 미완료 → '최근 한 일'로 이동, 오늘엔 없음 (중복 없음)
- [ ] 본인 외 팀원 할일은 수정/삭제 불가 (readonly)

### 실시간 동기화 (2개 창/기기로 점검)
- [ ] 한 쪽에서 작성 → 다른 쪽에 반영, **입력 중이던 텍스트는 안 날아감**
- [ ] 같은 항목 동시 편집 시 커서/포커스 유지
- [ ] 오프라인 → 온라인 복귀 시 동기화

### 권한
- [ ] 옵저버: 모든 쓰기 차단(화면만), DB 미반영
- [ ] 일반 팀원: OKR 구조 변경 불가, 본인 항목만 편집
- [ ] 관리자: 전체 편집 + 관리자 탭

### OKR/WBS
- [ ] Init 날짜 변경 → 부모 KR·O 기간 자동 확장
- [ ] WBS 범위가 분기 밖 항목까지 확장, 빈 과거 절단
- [ ] 진척률 색상(70/30 경계), 0목표 나눗셈 안전

### 기타
- [ ] 한글 IME 조합 중 Enter/단축키 오동작 없음
- [ ] 모바일/태블릿 터치로 날짜·체크·드래그 정상
- [ ] Excel 내보내기·리포트 출력 정상

> 수동 점검에서 새 버그 발견 시 → 가능하면 **자동 테스트 케이스로 추가**해 재발 차단.
